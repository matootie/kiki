{
  "name": "kiki-release-notifier",
  "description": "Sends out webhook notifications when new versions of Kiki are released.",
  "scripts": {
    "package": "ncc build index.js -o dist"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@actions/core": "^1.1.1",
    "@actions/github": "^3.0.0",
    "request-promise-native": "^1.0.8"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  }
}
